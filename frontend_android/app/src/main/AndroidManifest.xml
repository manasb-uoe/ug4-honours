<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.enthusiast94.edinfit" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- The Google Maps Android API uses OpenGL ES version 2 to render the map.
 If OpenGL ES version 2 is not installed, map will not appear. Therefore, this is needed to
 prevent that from happening: -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.login_and_signup.activities.LoginActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ui.home.activities.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.user_profile.activities.UserProfileActivity"
            android:label="@string/title_activity_user_profile" >
        </activity>
        <activity
            android:name=".ui.stop_info.activities.StopActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ui.service_info.activities.ServiceActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ui.wair_or_walk_mode.activities.NewActivityActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ui.wair_or_walk_mode.activities.ResultActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".ui.find_a_bus.activities.FindABusActivity"
            android:label="@string/app_name" >
        </activity>

        <service android:name=".ui.wair_or_walk_mode.services.CountdownNotificationService" />

        <!-- This embeds the version of Google Play services that the app was compiled with. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"
            />

        <!-- Google Maps v2 API API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key_google_maps"
            />
    </application>

</manifest>
